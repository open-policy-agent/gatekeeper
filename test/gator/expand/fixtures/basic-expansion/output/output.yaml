apiVersion: v1
kind: Pod
metadata:
  annotations:
    owner: admin
  labels:
    app: nginx
  name: mock-pod-nginx-deployment
spec:
  containers:
  - args:
    - /bin/sh
    image: nginx:latest
    imagePullPolicy: Always
    name: nginx
    ports:
    - containerPort: 80
