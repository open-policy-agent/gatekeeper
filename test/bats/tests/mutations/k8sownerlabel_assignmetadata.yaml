apiVersion: mutations.gatekeeper.sh/v1alpha1
kind: AssignMetadata
metadata:
  name: k8sownerlabel
spec:
  match:
    scope: Cluster
    kinds:
      - apiGroups: [""]
        kinds: ["Namespace"]
  location: "metadata.labels.owner"
  parameters:
    assign:
      value: "gatekeeper"
