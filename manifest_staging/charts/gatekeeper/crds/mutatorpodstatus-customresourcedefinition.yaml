---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  labels:
    gatekeeper.sh/system: "yes"
  name: mutatorpodstatuses.status.gatekeeper.sh
spec:
  group: status.gatekeeper.sh
  names:
    kind: MutatorPodStatus
    listKind: MutatorPodStatusList
    plural: mutatorpodstatuses
    singular: mutatorpodstatus
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: MutatorPodStatus is the Schema for the mutationpodstatuses API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          status:
            description: status is the observed state of the mutator for this pod.
            properties:
              enforced:
                description: enforced indicates whether the mutator is currently enforced
                  on this pod.
                type: boolean
              errors:
                description: errors lists any errors encountered while adding the
                  mutator on this pod.
                items:
                  description: MutatorError represents a single error caught while
                    adding a mutator to a system.
                  properties:
                    message:
                      description: message is a human-readable description of the
                        error.
                      type: string
                    type:
                      description: |-
                        type indicates a specific class of error for use by controller code.
                        If not present, the error should be treated as not matching any known type.
                      type: string
                  required:
                  - message
                  type: object
                type: array
              id:
                description: id is the name of the pod that generated this status.
                type: string
              mutatorUID:
                description: |-
                  mutatorUID is the UID of the mutator this status reports on. Storing the
                  mutator UID allows us to detect drift, such as
                  when a mutator has been recreated after its CRD was deleted
                  out from under it, interrupting the watch
                type: string
              observedGeneration:
                description: |-
                  observedGeneration is the generation of the mutator that was last processed
                  by this pod.
                format: int64
                type: integer
              operations:
                description: |-
                  operations lists the Gatekeeper operations assigned to the pod that generated
                  this status.
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
