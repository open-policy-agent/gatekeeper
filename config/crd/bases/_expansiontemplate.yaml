---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: expansiontemplate.
spec:
  group: ""
  names:
    kind: ExpansionTemplate
    listKind: ExpansionTemplateList
    plural: expansiontemplate
    singular: expansiontemplate
  scope: Cluster
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: ExpansionTemplate is the Schema for the ExpansionTemplate API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ExpansionTemplate.
            properties:
              applyTo:
                description: |-
                  applyTo lists the specific groups, versions and kinds of generator resources
                  which will be expanded.
                items:
                  description: |-
                    ApplyTo determines what GVKs the resource applies to.
                    Globs are not allowed.
                  properties:
                    groups:
                      items:
                        type: string
                      type: array
                    kinds:
                      items:
                        type: string
                      type: array
                    versions:
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              enforcementAction:
                description: |-
                  enforcementAction specifies the enforcement action to be used for resources
                  matching the ExpansionTemplate. Specifying an empty value will use the
                  enforcement action specified by the Constraint in violation.
                type: string
              generatedGVK:
                description: |-
                  generatedGVK specifies the GVK of the resources which the generator
                  resource creates.
                properties:
                  group:
                    description: group is the API group of the generated resource.
                    type: string
                  kind:
                    description: kind is the kind of the generated resource.
                    type: string
                  version:
                    description: version is the API version of the generated resource.
                    type: string
                type: object
              templateSource:
                description: |-
                  templateSource specifies the source field on the generator resource to
                  use as the base for expanded resource. For Pod-creating generators, this
                  is usually spec.template
                type: string
            type: object
          status:
            description: status is the observed state of ExpansionTemplate.
            properties:
              byPod:
                description: byPod lists the observed status of this ExpansionTemplate
                  for each pod.
                items:
                  description: ExpansionTemplatePodStatusStatus defines the observed
                    state of ExpansionTemplatePodStatus.
                  properties:
                    errors:
                      description: errors lists any errors encountered while processing
                        the ExpansionTemplate.
                      items:
                        properties:
                          message:
                            description: message is a human-readable description of
                              the error.
                            type: string
                          type:
                            description: |-
                              type indicates a specific class of error for use by controller code. If not
                              present, the error should be treated as not matching any known type.
                            type: string
                        required:
                        - message
                        type: object
                      type: array
                    id:
                      description: id is the name of the pod that generated this status.
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration is the generation of the ExpansionTemplate that was last
                        processed by this pod.
                      format: int64
                      type: integer
                    operations:
                      description: |-
                        operations lists the Gatekeeper operations assigned to the pod that generated
                        this status.
                      items:
                        type: string
                      type: array
                    templateUID:
                      description: |-
                        templateUID is the UID of the ExpansionTemplate this status reports on, used
                        to detect drift, such as when the ExpansionTemplate has been recreated after
                        its CRD was deleted out from under it, interrupting the watch.
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
