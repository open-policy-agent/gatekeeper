---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: constraintpodstatuses.status.gatekeeper.sh
spec:
  group: status.gatekeeper.sh
  names:
    kind: ConstraintPodStatus
    listKind: ConstraintPodStatusList
    plural: constraintpodstatuses
    singular: constraintpodstatus
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: ConstraintPodStatus is the Schema for the constraintpodstatuses
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          status:
            description: status is the observed state of the Constraint for this pod.
            properties:
              constraintUID:
                description: |-
                  constraintUID is the UID of the Constraint this status reports on. Storing
                  the constraint UID allows us to detect drift, such as
                  when a constraint has been recreated after its CRD was deleted
                  out from under it, interrupting the watch
                type: string
              enforced:
                description: enforced indicates whether the Constraint is currently
                  enforced on this pod.
                type: boolean
              enforcementPointsStatus:
                description: |-
                  enforcementPointsStatus reports the enforcement status of the Constraint for
                  each configured enforcement point.
                items:
                  description: EnforcementPointStatus represents the status of a single
                    enforcement point.
                  properties:
                    enforcementPoint:
                      description: enforcementPoint identifies the enforcement point
                        this status applies to.
                      type: string
                    message:
                      description: message provides additional detail about the enforcement
                        point state.
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration is the generation of the Constraint that was last processed
                        for this enforcement point.
                      format: int64
                      type: integer
                    state:
                      description: state is the current enforcement state for this
                        enforcement point.
                      type: string
                  required:
                  - enforcementPoint
                  - state
                  type: object
                type: array
              errors:
                description: errors lists any errors encountered while adding the
                  Constraint on this pod.
                items:
                  description: Error represents a single error caught while adding
                    a constraint to engine.
                  properties:
                    code:
                      description: code is a short identifier for the class of error.
                      type: string
                    location:
                      description: location is the resource or component associated
                        with the error.
                      type: string
                    message:
                      description: message is a human-readable description of the
                        error.
                      type: string
                  required:
                  - code
                  - message
                  type: object
                type: array
              id:
                description: id is the name of the pod that generated this status.
                type: string
              observedGeneration:
                description: |-
                  observedGeneration is the generation of the Constraint that was last processed
                  by this pod.
                format: int64
                type: integer
              operations:
                description: |-
                  operations lists the Gatekeeper operations assigned to the pod that generated
                  this status.
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
