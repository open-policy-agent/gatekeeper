# Adds namespace to all resources.
namespace: gatekeeper-system

resources:
- ../../default

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
patches:
- patch: manager_image_patch.yaml
